path: 
  github: "https://github.com"
    top: "/{user}/{repository_name}"
    branch: "/{user}/{repository_name}/tree/{branch_name}"
    branchs: "/{user}/{repository_name}/branchs" 
    commits: "/{user}/{repository_name}/commits"
    pull_requests: "/{user}/{repository_name}/pulls"
    # issues: "/{user}/{repository_name}/issues"
  bitbucket: "https://bitbucket.org"
    top: "/{user}/{repository_name}"
    branch: "/{user}/{repository_name}/branch/{branch_name}"
    branchs: "/{user}/{repository_name}/branchs" 
    commits: "/{user}/{repository_name}/commits"
    pull_requests: "/{user}/{repository_name}/pull-requests"